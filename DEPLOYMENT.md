# 🚀 Deployment Guide

Build the site with Vite and deploy the generated `dist/` folder.

## ✅ Build

```bash
npm install
npm run build
```

## 🌐 Deploy

- **GitHub Pages**: deploy the `dist/` folder via a Pages workflow or a static branch.
- **Netlify**: set the publish directory to `dist/`.
- **Vercel**: set the output directory to `dist/`.

## 🔧 Quick Checks

- Verify navigation and section links.
- Confirm contact details and social links.
