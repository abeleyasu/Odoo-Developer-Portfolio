# GitHub Pages Setup

This project uses Vite, so deploy the `dist/` folder.

## Steps

1. Run `npm run build`.
2. Publish the `dist/` folder using GitHub Pages (via a workflow or a dedicated branch).
