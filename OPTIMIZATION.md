# Optimization Notes

This project uses Vite for bundling and Tailwind for styling. Use the Vite build output to keep assets optimized.

## Recommendations

- Run `npm run build` before deploying.
- Serve the `dist/` folder with gzip or brotli enabled in your hosting platform.
